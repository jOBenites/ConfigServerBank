spring:
  data:
    mongodb:
      uri: mongodb://devuser:devpassword@localhost:27017/auth_db?authSource=admin

server:
  port: 8090

auth:
  jwt:
    issuer: http://auth-service:8090
    secret: ${JWT_SECRET}
    expiration-minutes: 60
